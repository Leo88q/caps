# Fixes 2026-09-23 · закрытие остатков аудита 2026-09-21 (F-02 / F-05 / F-06 / F-12 / F-14 / F-19)

Продолжение `FIXES-2026-09-21.md`: шесть пунктов, которые там были помечены «отложено / рекомендация»,
переведены в код, CI и документацию. Каждый пункт — что было, что сделано, чем проверено.

| # | Было | Сделано | Файлы | Проверка |
|---|------|---------|-------|----------|
| **F-02** оракулы без алертов | `burnOracleStatus.healthy` / `rewardOracleStatus.healthy` жили только в `/v1/health`; остановка burn-кипера = эмиссия молча к полу 30 % | Gauges `burn_oracle_{report_age_seconds,pending_cg,healthy}`, `reward_oracle_{publish_age_seconds,pending_batches,oldest_pending_age_seconds,healthy,unrooted_cg{kind}}` + правила `BurnOracleStale`, `RewardOracleStale`, `RewardOracleSilent` (последнее ловит слепое пятно `healthy` — мёртвый воркер не строит батчей) | `backend/src/oracle-metrics.ts`, `backend/src/server.ts`, `ops/monitoring/alerts.yml`, `ops/deploy/runbook.md` §3.1 | `backend/test/oracle-metrics.test.ts`, контракт `monitoring.test.ts` (каждая серия из alerts.yml экспортируется) |
| **F-05** `guard-mainnet` только доступен | Команда была, но её никто не вызывал | (а) `scripts/setup.ts` на mainnet-RPC сам отказывается работать, если эффективные id совпадают с `[programs.devnet]`/`[programs.localnet]` из `Anchor.toml` (Anchor.toml парсится локально — `program-ids.ts` не модуль); (б) `images.yml` вызывает `program-ids -- guard-mainnet` для `cluster: mainnet-beta` | `scripts/setup.ts` (`mainnetPreflight`), `.github/workflows/images.yml`, runbook §1.1 | ручной прогон `DRY_RUN=1` с mainnet-URL → `mainnet pre-flight (SEC-F05) FAILED` с 8 строками; `workflows:check` |
| **F-06** oracle cap 1 M $CG/день + нет канарейки | Круглый дефолт без связи с оборотом; чужое использование ключа `battle_oracle` неотличимо от своего | (а) дефолт = `ARENA_ORACLE_DAILY_CAP_DEFAULT_CG` = `pvpDailyPotVolumeCg()` = **120 000 $CG/день** (модельный дневной объём потов на baseline 5 000 DAU; та же формула теперь и в `dailyFlows`); (б) канарейка `arena_unattributed_resolves` — `BattleResolved`, подпись которого нет в `matches.resolve_sig` (battle-resolver пишет её после каждого своего `sendAndConfirm`; `matches` не проекция, rebuild не сбивает) → алерт `ArenaResolveNotOurs` (page); (в) `arena_oracle_{cap_cg,paid_today_cg,cap_readable}` с чейна (кэш 30 с, только при `BATTLE_ORACLE_KEYPAIR`) → `ArenaOracleCapNearlyExhausted` при 80 % | `packages/economy/src/tokenomics.ts`, `scripts/setup.ts`, `backend/src/oracle-metrics.ts`, `backend/src/db.ts` (индекс `idx_matches_resolve_sig`), `backend/openapi.yaml` + `client/src/api/schema.d.ts` (`/health.arena.unattributedResolves`), `docs/02` §4.6 | economy-тест «arena oracle cap default», `oracle-metrics.test.ts` (окно 24 ч, NULL block_time, атрибуция, кэш, лениво сброшенное окно), `api.test.ts` |
| **F-12** 23 npm-уязвимости (6 high) | Все «ломающие» — на деле три корня: `jayson` (→ `uuid`, `stream-json`), `toml`, `bigint-buffer` | `overrides`: `jayson ^5.0.0` (web3.js берёт `jayson/lib/client/browser`, в 5.x он на месте, `uuid`/`stream-json` убраны) и `toml ^5.0.0` (тот же `parse(input)`, нужен anchor 0.30/0.31 для `Anchor.toml`). Итог: **23 → 4 записи одной цепочки `bigint-buffer`** (GHSA-3gc7-fjrx-p6mg, апстрим-фикса нет). Новый `scripts/audit-gate.ts`: `npm audit --omit=dev --json` → обход цепочек → **падение** на любом high/critical вне датированного `ACCEPTED` (истёкшая запись — тоже падение, неиспользуемая — предупреждение); джоб `security` переведён с «warning + critical» на этот гейт | `package.json` (overrides, `audit:gate`, `verify`), `package-lock.json`, `scripts/audit-gate.ts`, `.github/workflows/ci.yml`, `SECURITY.md`, `docs/09` §5.6 | `audit:gate --selftest` (4 кейса), `audit:gate` → OK; `lock:matrix`; client 137 / backend 331 тестов; client build + `bundle:check` (296 KB ≤ 350) |
| **F-14** три правила отряда | Core: staked может; compressed v1: без проверки staked/minted; v2: `!staked` | Одно правило, задокументировано в `docs/02` §4.6: владелец, не listed, не в fusion, **staked может драться** (как обещает UI стейкинга и SEC-L2); `minted && registered` в v2 — требование источника данных (проверка листа), не собственности. Код: из `validate_compressed_squad_v2` убран `!claim.staked`; комментарии во всех трёх путях ссылаются на правило | `programs/arena/src/lib.rs`, `docs/02-economy.md`, `tests/localnet/40-arena.spec.ts` (A08: стейк claim → бой создаётся), `tests/localnet/README.md` | `tsc -p tests/localnet`; localnet A08 — в CI (LiteSVM) |
| **F-19** артефакт деплоя не проверяется | `--features devnet|localnet` даёт бинарь с чужим Switchboard (для localnet — `sb_mock`, keypair которого лежит в репо); id программы тот же, тесты зелёные | `scripts/verify-deploy.ts`: `artifact --cluster …` (скан `.so` на 32-байтовые пины `SB_PROGRAM_ID`/`SB_QUEUE`: свои — целиком, чужие — ни одного 8-байтового чанка; понимает `.rodata` и `lddw`-иммедиаты), `onchain --cluster … --rpc …` (Program → ProgramData, slot, upgrade authority, sha256, побайтовое сравнение с локальным `.so`, тот же скан), `--selftest` (таблица пинов ⇄ `randomness.rs`, синтетические бинари, layout загрузчика). CI: selftest в `backend`-джобе, `artifact --cluster localnet` на реальном артефакте в `localnet`-джобе; `setup.ts` на mainnet делает `onchain`-проверку chip_core + arena до первого шага | `scripts/verify-deploy.ts`, `scripts/setup.ts`, `.github/workflows/ci.yml`, `package.json` (`verify-deploy`), runbook §1.1.1 | `verify-deploy --selftest` (5 кейсов); прогон на артефакте — CI |

## Верификация

CI run **35896402533** на `d9417ee`: все 8 обязательных джобов зелёные (economy · client · e2e · backend · landing · programs fmt+anchor build · rust clippy+tests · security через `audit:gate`) + `localnet · 77 scenarios` (A08 со стейкнутым claim, `verify-deploy artifact --cluster localnet` на собранных `.so`).
CI run на `arena/01a0cf4d-guttercaps`: все 8 джобов синхронизированы, rustfmt применён из контейнера `solanafoundation/anchor:v0.31.1`.

## Что осталось владельцу

- Alertmanager по-прежнему не подключён (runbook §3.2) — новые правила читаются как мини-runbook в `annotations`, доставка — `docs/09 §7`.
- `bigint-buffer`: запись в `scripts/audit-gate.ts` истекает **2027-03-31**; снять её можно, когда `@solana/spl-token`/`@solana/buffer-layout-utils` уйдут с него (линия web3.js 2.x / kit).
- ~~Гипотеза «крейт `switchboard-on-demand` не линкует чужие id в наш `.so`»~~ — подтверждена первым же прогоном: шаг `deploy artifact carries exactly the localnet Switchboard pins` зелёный на реальном артефакте (CI run 35896402533, коммит `d9417ee`): пины `sb_mock` + очереди найдены целиком, ни одного чанка `SBond…`/`Aio4…`/`A43D…`.
- `ORACLE_DAILY_CAP_CG` в уже развёрнутых окружениях (devnet) остаётся прежним — дефолт влияет только на новый `init_arena`; поднять/опустить — `set_arena` (`backend/src/admin.ts`).
