# Visual pass 2026-09-25 · свои иконки вместо эмодзи/динбатов + спокойный цвет

Полный визуальный проход: панель наград, иконки сервисов/элементов/действий,
типографика и цвет — в игре и на лендинге. Правило прохода: **эмодзи и текстовые
динбаты — не фирменный стиль** (на Android/Windows они рисуются чужим шрифтом и
ломают палитру), а **неон в цвете текста читается как крик** — он остался только
в «краске» (кнопки, рамки, потёки), текст и глифы получили спокойные варианты.

## 1. Новый векторный набор (клиент)

Все иконки строятся по правилам дизайн-брифа (Block 6): 2–3 слоя фигур, один
idle-цикл < 2.4 с, one-shot активация по тапу — там, где уместно. Цвета — только
из токенов `theme.css`.

| Файл | Что внутри | Заменяет |
|---|---|---|
| `shared/ui/reward-icons.tsx` + `.css` | `$CG` (крышка-монета с блеском), `$SKR` (колесо с подшипником), booster (баллон с манометром), voucher (надорваный талон), streak (граффити-пламя), stash (ящик с крышкой) | текстовые `?`/«+N CG» в панели наград |
| `shared/ui/element-icons.tsx` | 5 стихий: paint (мазок с потёками), steel (гайка), wheels (колесо), noise (мегафон), shadow (полумесяц) | `ELEMENT_ICON` эмодзи 🎨⚙️🛞🔊🌑 из `rarity.ts` (5 экранов: Arena, MatchReplay, ChipDrawer, Collection, ChipPage) |
| `shared/ui/service-icons.tsx` | 10 услуг каталога «Extras» | текстовые динбаты `@ ↻ ◐ ▦ ✦ ⊞ ★ ⚡ » ⚑` в `Services.tsx` |
| `shared/ui/action-icons.tsx` | copy, external, close, check, cross, alert, chevron, server, shield, logout, wrench, motion, trash, spark, lang | `✕ ✓ ✗ ⚠ ↗ →` по всему приложению (Fusion, Collection, Admin, Verify, Profile, Shop, PackStepper, тосты, чек-стамп) |

Языковые флаги-эмодзи (🇬🇧🇧🇷🇪🇸🇮🇻🇳🇮🇩🇵🇭🇷🇺) заменены на трафаретную
табличку с кодом языка (`LangTag` в `Language.tsx`) — флаги рисуются эмодзи
шрифтами платформы и выбивались из палитры. Косметические пропуски: флаг в
`LOCALE_META` остался в типе, но нигде не рисуется.

## 2. Панель наград (Quests)

- Стрик: иконка-пламя, прогресс в тоне `orange` (был `acid`).
- «Ready to claim» — сводка собирается в **чипы наград** (`.reward-chips`):
  монета `$CG` / колесо `$SKR` / бустер / ваучер + mono-сумма, вместо простого
  `KV total`.
- Каждая Merkle-строка получила лицо своего типа награды (`kindGlyph`).
- Слот ваучера (`disc-slot`) вместо «?» носит векторный ваучер.
- В списке квестов каждая награда — глиф + сумма (`.quest-reward`), а не «+N CG»
  сплошным текстом.
- Карта human-check — иконка-щит.

## 3. Иконки нажимаемого

- В шапке рядом с кошельком — кнопка настроек (`SettingsIcon`, вал 18px, тестид
  `settings-link`); сама иконка настроек в `icons.tsx` уже была (крутилка-колпачок).
- Переключатели настроек профиля (звук/пониженное движение/мгновенный reveal)
  получили иконки (`SprayCapToggle` принял `icon`), у `Stat` появился `icon`.
- Все внешние ссылки (explorer, on-ramp, реферальные) — `ExternalIcon` вместо ↗,
  «See all» / «Read the lore» — `ChevronRightIcon` вместо →.
- Кнопка clear в панели фильтров, удаление пресетов сплава, крест слота — `CloseIcon`.

## 4. Спокойный цвет (игра)

- В `theme.css` добавлены **soft-варианты** палитры: `--cg-{magenta,cyan,acid,
  orange,trust}-soft` (те же hue, ~70 % насыщенности, светлее). Правило: неон —
  в формах (краска, кнопки, рамки), текст — soft или chrome.
- `RARITY_COLOR` переведён на soft-варианты (имена редкостей рендерятся цветным
  текстом в сетках/дроверах — неон вибрировал на асфальте).
- Заголовки (`page-title`, `modal-title`, `shell-brand`): хром + плоская тень
  вместо трёхслойного неонового свечения цветом акцента; акцент остался в потёке
  и рамке (`.page-title::after` приглушён).
- `street-apply.css`: снят text-shadow с кнопок, свечение кард/рамок/кнопок/прогрессов
  снижено примерно вдвое, активный таб навигации — хром с мягким ореолом, разделители
  шапки/навигации приглушены, `.label`/таблицы — на Inter вместо маркерного шрифта.
- Инлайн-цвета текста в фичах (`neon-cyan/magenta/acid/electric-orange/trust`)
  переведены на soft-варианты; `.warn/.danger/.ok` и пилюли — приглушены;
  `--gc-muted` чуть светлее (#9b96a3) — контраст без крика.

## 5. Лендинг (`guttercaps-landing.html`)

- 6 эмодзи-иконок механик (⚗️🥊💰🏷️📋🎨) заменены на фирменные SVG (сплав,
  слэм, стейк-автомат, банки, летов, баллон) в той же палитре; `⚡`/`⚖` в карточках
  тиров — маленькие SVG-болт/весы.
- Добавлены те же soft-токены; цветные заголовки/акценты секций, eyebrow, факты
  механик, ссылки и статистика переведены с неона на soft/chrome; свечение
  шагов «how to» и угловое свечение паков снижено; `.value-item h3` и
  `.stat-num` — хром; peak-строке Diamond убран text-shadow (было свечение).
- Проверка: встроенные `<script>` парсятся без ошибок (первый и второй блоки и
  до правки имели артефакты извлечения — не от этого прохода).

## 6. Сгенерированные иконки (AI, стиль арта крышек) — 2026-09-25, второй заход

Эмодзи/динбаты заменены не только векторными фигурами, но и **сгенерированными
бейджами** в стиле трафаретного спрея (как арт крышек `client/public/art`):
исходники `art_drafts/icons/gen/*-raw.png` (1024px), обработка sharp → квадрат →
круглая альфа-маска → `client/public/icons/gen/*.webp` 96px (~3–4 КБ).

Готово (10 бейджей):
- стихии: `paint steel wheels noise shadow` — `ElementGlyph` рендерит webp;
- награды: `cg skr booster voucher streak` — `CgCoinIcon/SkrTokenIcon/
  BoosterIcon/VoucherIcon/StreakIcon` рендерят webp (класс `.gic`, лёгкая
  drop-shadow, inline-выравнивание).

Дальше (второй батарь, 10 генераций):
- ✅ `stash` — готов (лаймовый ящик), все 6 наград теперь генерированные;
- ✅ `skr` перегенерирован — стопка колёс, от `wheels` больше не отличить путается;
- ✅ 6 иконок механик лендинга: `mech-{fusion,slam,staking,market,quests,extras}` —
  вставлены в `guttercaps-landing.html` как base64 data-URI (64px, круглые);
- ✅ первые 2 услуги Extras: `svc-pass` (ламинат), `svc-skin` (сплит-крышка) —
  `ServiceGlyph` рендерит их webp, остальные 8 — векторные заглушки;
- action-иконки намеренно остаются векторными (штриховые контуры): это
  элементы управления 14px, штамп-текстура на них читаться не будет.

## 7. Третий батарь: навигация и главный экран (2026-09-25)

Фидбек: «в игре осталось всё старое» — ген-иконки сидели на внутренних
экранах, а самое видное (нижняя навигация, лендинг-хиро) было на векторных
wicon. Исправлено:

- 8 новых бейджей `nav-*` (64px): home (баллон), caps (рюкзак с крышкой),
  shop (фольг-пак), market (чокающиеся баллоны), arena (доски крест),
  stake (автомат), collect (сетка 3×3), board (подиум с короной);
- нижняя навигация (`Shell.tsx`): 6 из 7 вкладок на ген-бейджах (`nav-img`),
  неактивные приглушены (grayscale+brightness), активная — в цвете с мягким
  ореолом; язык остался векторным (там код локали);
- главный экран: карточки Daily quests (mech-quests) и Season (nav-board),
  лендинг-карточки Collect/Fuse/Slam (nav-collect / mech-fusion / mech-slam);
- общий хелпер `shared/ui/GenBadge.tsx` (`<GenBadge name size />`).

## Верификация

- `tsc --noEmit` — 0 ошибок; `vitest` — **151/151**; `vite build` — ок.
- Эмодзи/динбаты в живом UI: `perl -CSD` скан `client/src` вне локалей —
  только комментарии (`rarity.ts` — историческая справка, наборы иконок — доккомменты).
- Превью: игра `npm run dev` (5173, mock API), лендинг — статика (8080).

## 8. Четвёртый батч: чипы районов, фоны секций, фото шагов, слоты составов (2026-09-25)

Запрос: «задымились фишки в каждой коллекции и размазались — убери некрасивые;
не везде есть фоновые картинки; цифры 12345 — сгенерируй фото под них; в игре
сделай 3 кружка (Your squad) с фото, но чтобы „+“ нажимались».

### Диагноз «дыма/размазывания»
- Лендинг инлайнил ИГРОВЫЕ контактные листы районов, даунскейленные до тайла
  160px (лист 528px) — при отрисовке чипа 138px (scale≈0.86) и баннера это
  давало мыло/«дым»; плюс баннер `.district-art` имел `width:132px` при
  валидном HTML-атрибуте `height="528"` → картинка растягивалась в овал.
- Свечение CSS (`.chip-circle --glow`) не «дымило» — источник был в самом арте.

### Исправления
- Контактные листы пересобраны из ЧИСТЫХ источников `client/public/art/NN-r-256.webp`
  (9 тайлов 256px, gut 12, pad 16 → лист 824px, q56, ~70–85КБ × 8 районов);
  `DISTRICT_ART_GEO { tile: 256, gut: 12, pad: 16 }`; атрибуты `width/height="824"`.
- `.district-art`: `height:auto; aspect-ratio:1` — фикс растяжения баннера
  (в т.ч. на мобильной сетке).
- Фоны секций: `.wall-photo` переключён с `background-size: contain` на `cover`
  (в высоких секциях фото было узкой полосой по центру — «не везде есть фоновые
  картинки»), средняя точка вуали затемнения поднята 0.16→0.30 для читаемости.
- Шаги 1–5 (how-to): сгенерированы 5 ночных «кинематографичных» фото
  (подключение кошелька / паки на ящике / вскрытие фишки под лучом / фьюжн с
  искрами / скейт-батл), 560px webp → `STEP_ART` + `STEP_KEYS`, рендер
  `<img class="step-photo">` над кольцом с цифрой; стили в `#gen-photo-addons`.
- Игра, Cap Slam «Your squad»: пустые слоты — ген-арт `/icons/gen/arena-slot.webp`
  (круг-трафарет на асфальте, 128px) через `.squad-slot-empty`, «+» —
  `.squad-slot-plus`; кликабельный обёрточный div не тронут — «+» нажимается.

### Новые исходники
- `art_drafts/icons/gen/step-{connect,pack,reveal,fuse,play}-raw.png`,
  `arena-slot-raw.png`; также запасные `wall-{rules,events,stats}-raw.png` —
  не инлайнились: у всех секций уже есть живые фото, оставлены как резерв.

### Верификация
- tsc 0; vitest 151/151; парс лендинга (DISTRICT_ART/STEP_ART JSON + все скрипты)
  — ок; `arena-slot.webp` на dev-сервере — 200.
- Превью-монтаж листа 01 / step-connect / arena-slot — визуально чисто.
