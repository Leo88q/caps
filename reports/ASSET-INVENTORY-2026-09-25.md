# Полная инвентаризация ассетов · GUTTERCAPS · 2026-09-25

Ревизия всего визуального контента репозитория: мастеры фишек, производные для игры,
фоны, баннеры, иконки, лендинг, пакет для dApp Store и экспорты для NFT.

Метод: всё, что измеримо, измерено скриптами, а не «на глаз» —
`scripts/asset-inventory.py` (геометрия/формат/покрытие) и `scripts/asset-visual-qa.py`
(цвет обода, палитра районов, различимость тиров, контраст на малых размерах).
Сырые метрики: `reports/asset-inventory.json`, `reports/asset-visual-qa.json`.
Контактные листы для ручного просмотра: `reports/contact-masters.png` (все 72, 150 px),
`reports/contact-56.png` (тест читаемости 56 px из §2.1).

Проверено на собранном бандле: `VITE_API_MOCK=1 vite build` → `bundle:check` (296.5 КБ gzip ≤ 350 КБ бюджета), `landing:check` (все проверки пройдены),
и на прогнанном through-HTTP `vite preview` — каждый ассет отвечает 200.

> **Апдейт 2026-09-25 (вторая итерация).** Отчёт перепроверен с нуля в чистой песочнице:
> оба скрипта прогнаны заново (результаты идентичны), все 170 публичных файлов клиента
> отвечают 200 по HTTP с корректным MIME, `landing:check` и `bundle:check` зелёные.
> Закрыты оба P0-блокера из §9: собраны **`client/public/og.png`** и
> **`scripts/landing/assets/og-guttercaps.png`** (1200×630, коллаж из существующих
> мастеров + `bg/game-home.jpg`, без новой генерации — см. §8.1) и скопирована иконка
> **`dapp-store/media/icon-512.png`** (512², RGB без альфы — требование стора соблюдено,
> §8.3).
>
> **Апдейт 2026-09-25 (седьмая итерация).** **Вопрос №3 плана решён владельцем — «перекрашивать кадры»:**
01/02/06 приведены к палитре лора дуотон-скриптом `scripts/art-recolor-districts.py`
(27 мастеров, оригиналы в `raw/{…}_v3.png`, обод 92–100 % R не тронут): 01 — тёмный
асфальт + циановый мотылёк, 02 — янтарь/оранж, 06 — cyan. Производные пересобраны.
Также: **§6 закрыт (12/12)** — паки `premium`/`limited` готовы; сделаны **иконки токенов
$CG и $SKR** (1024→32, `client/public/tokens/`, копии в `exports/cdn/tokens/` — для
кошелька и биржи), **набор иконок браузера** (новый `favicon.svg`-мотылёк + PNG 48/32/16 +
`favicon.ico`, подключены в `index.html`), **hero-обложка коллекции** для внешнего
маркетплейса (`exports/cdn/collection/hero.png` 2048² + `hero-1500x500.jpg`).

> **Апдейт 2026-09-25 (восьмая итерация).** P1-остаток лестницы ободов §3 закрыт
(зональная правка пояса 92–100 % R, скрипт `scripts/art-rim-fix.py`, оригиналы
`raw/{…}_v4.png`): `02-4` серый → маджента-эмаль s66, `02-2` розовый → сталь s4,
`03-3` серый → бензин s28 (h196). Теперь все 8材料 тиров читаются по ободу во всех
районах. Скриншоты стора: браузеры в песочнице недоступны (CDN заблокированы) —
остаются единственным медиа-пунктом для владельца, в `PORTAL_CHECKLIST.md`
добавлена 5-минутная инструкция снятия с локальной сборки.

> **Апдейт 2026-09-25 (шестая итерация).** Начат §6 спеки: сгенерированы **8 обложек
районов 2048²** (`exports/cdn/covers/{num}.png` — сцены по `lore.ts`, без дисков и
UI-текста) и **2 из 4 паков** (`exports/cdn/packs/{starter,standard}.png`, 1536×2048,
3:4); `premium` и `limited` — следующий ход (лимит генераций на ход). Источники
1024²/896×1200 лежат в `art_drafts/{covers,packs}/`, в экспорт апскейлятся ×2
(LANCZOS + unsharp) — до пересдачи художником в нативном разрешении это черновое
качество, меток «final» на них нет.

> **Апдейт 2026-09-25 (пятая итерация).** Закрыт измеримый остаток арт-дирекшна (P2.4–6):
**тиры-близнецы** перегенерированы по референсу и дочинены (04-3 «Triple Pass», 05-1 «Encore»,
07-1 «Continue? 9» — RMS пар 5.1/9.0/7.8 → **99.3/98.8/100.0**; вскрытая свежим QA пара
07-6/07-7 «Repeat» — 10.4 → **96.9**); **Legend (тир 6)** поднят γ-лифтом (+20–25 % яркости,
двойной проход для 03-6/03-7/04-6/10-6) — больше не самый глухой тир; **обод Diamond** —
хром-голограмма с цианом (sat 13–37 → **39–42**, секторная маджента-вспышка). Скрипт —
`scripts/art-retouch-p2.py` (одноразовый, мастера восстановимы из git), контакт-листы —
`scripts/asset-contact-sheets.py`. Открытым из арт-дирекшна остался только P1-вопрос
владельца (цвет районов 01/02/06 vs lore, материал ободов 2–4).

> **Апдейт 2026-09-25 (четвёртая итерация).** Закрыты `animation_url`-видео (§7):
24 лупа × {webm, mp4}, 1024², 1.6 с, ≤263 КБ — процедурно из карточек (спека §4 это
разрешает), ffmpeg из pip-пакета `imageio-ffmpeg`; `npm run assets:pipeline -- --animation`.
Все экспорты пайплайна помечены sRGB-профилем (D1). В клиент подключён **Inter**
(§6.3/P3) — body больше не рисуется системным шрифтом; бюджет 296.9/350 КБ.

> **Апдейт 2026-09-25 (третья итерация).** Написан и прогнан экспортный пайплайн §4/§5:
> `npm run assets:pipeline` → `exports/cdn/{game,nft,og,m}` — 216 webp (включая
> отсутствовавший третий размер 1024), 72 непрозрачные карточки NFT, 72 OG-превью,
> 72 metadata JSON из `lore.ts` (§7). Выровнены баннеры `districts/04.jpg` и `05.jpg`
> (§6.2). Добавлен `dapp-store/media/banner.png` 1200×600 (§8.3). Из измеримого
> остались только `animation_url`-видео (нет ffmpeg/слоёв), обложки/паки §6 и
> скриншоты устройства.

---

## 1. Вердикт

| Направление | Готовность | Комментарий |
|---|---|---|
| **Мастеры 72 фишки (геометрия/формат)** | ✅ **100 %** | 72/72, все пункты чек-листа §7 спеки выполняются |
| **Арт для игры (`/art/…-256|512.webp`)** | ✅ **100 %** | 144/144, бюджеты соблюдены, геометрия точная |
| **Фоны экранов (`/bg/`)** | ✅ готово | 13/13 подключены, битых ссылок нет |
| **Баннеры районов (`/districts/`)** | ✅ готово | 8/8, все 1200×509 (04/05 выровнены второй итерацией) |
| **Иконки PWA / dApp Store** | ✅ готово | 192/512 PNG без альфы — требование стора соблюдено |
| **Лендинг** | ✅ готово | 16 webp вшиты (0.92 МБ), `landing:check` зелёный; OG-картинка добавлена |
| **Мастеры: арт-дирекшн** | ✅ **готово** (вопросы лестницы закрыты; за владельцем — только нативные исходники обложек/кадров) | Близнецы, контраст Legend и обод Diamond закрыты (5-я итерация, §4–5); остался вопрос владельца: цвет районов 01/02/06 vs `lore.ts`, материал ободов 2–4 |
| **Экспорты пайплайна §4 (NFT, OG, анимации)** | ✅ **готово** | `npm run assets:pipeline -- --animation` → 216 webp в бюджетах + 72 карточки + 72 OG + 72 JSON + 48 видео (24×{webm,mp4}, ≤263 КБ); слои исходников — опционально для более богатой анимации |
| **Обложки районов и паки (§6)** | ✅ **12/12** (черновые) | 8 обложек 2048² + 4 пака 3:4; источники 1024², апскейл ×2 — финал за художником |
| **Медиа для Publisher Portal** | 🟡 **2/6** | `icon-512.png` + `banner.png` готовы; остались 4 скриншота 1080p с устройства |
| **OG-картинка приложения** | ✅ готово | `client/public/og.png` 1200×630 — собрана из существующих мастеров, по HTTP 200 `image/png` |

**Коротко:** игра готова к показу полностью — арт на месте, ничего не 404, бюджет соблюдён.
OG-картинки приложения и лендинга на месте. Экспортный пайплайн для минта написан и
прогнан (`exports/cdn/`, 72×{game,nft,og,m}, бюджеты соблюдены) — осталось
`animation_url`-видео и обложки/паки §6. Для стора не хватает только скриншотов.

---

## 2. Что лежит в репозитории

| Класс ассетов | Путь | Файлов | Объём | Статус |
|---|---|---:|---:|---|
| Мастеры фишек (2048² RGBA) | `art_drafts/master/` | 72 | 354 МБ | ✅ 72/72 по спеке |
| Черновики генератора | `art_drafts/raw/` | 74 | 186 МБ | 🗂 исходники, в прод не идут |
| Арт сайта (баннеры/фоны/иконка) | `art_drafts/site/` | 30 | 74 МБ | 🗂 исходники |
| Превью партий и вселенной | `art_drafts/*.png` | 10 | 17 МБ | 🗂 для сверки владельцем |
| Инструменты нарезки | `art_drafts/tools/` | 2 | — | `make_master{,_robust}.py` |
| Фишки для игры | `client/public/art/` | 144 | 7.4 МБ | ✅ 72 × {256, 512} |
| Фоны экранов | `client/public/bg/` | 13 | 1.8 МБ | ✅ все подключены |
| Баннеры районов | `client/public/districts/` | 8 | 884 КБ | ✅ |
| Иконки PWA | `client/public/icon-{192,512}.png` | 2 | 617 КБ | ✅ без альфы |
| Favicon | `client/public/favicon.svg` | 1 | 272 Б | ✅ |
| Ассеты лендинга (вшитые webp + OG) | `scripts/landing/assets/` | 17 | 1.4 МБ | ✅ 8 фонов + 8 листов районов + OG |
| Собранный лендинг | `guttercaps-landing.html` | 1 | 1.4 МБ | ✅ самодостаточный |
| OG-картинка приложения | `client/public/og.png` | 1 | 468 КБ | ✅ 1200×630 |
| Медиа dApp Store | `dapp-store/media/` | 2 | 1 МБ | 🟡 иконка + баннер; скриншоты — нет |
| Иконки токенов ($CG/$SKR) | `client/public/tokens/` | 12 | 1.5 МБ | ✅ 2 токена × 6 размеров (1024→32), копии в `exports/cdn/tokens/` для биржи/кошелька |
| Hero коллекции (маркетплейс) | `exports/cdn/collection/` | 2 | 6.2 МБ | ✅ 2048² + 1500×500 |
| Иконки браузера | `client/public/favicon*` | 5 | 16 КБ | ✅ SVG-мотылёк + PNG 48/32/16 + ICO, подключены в index.html |
| Веб-шрифты | `client/public/fonts/` | 0 | — | ⛔ папка пуста (см. §7.2) |
| Экспорты NFT / OG / метаданные | — | 0 | — | ⛔ не существуют |

Итого в git: ~630 МБ арта (`.git` — 644 МБ). Это не ошибка, но клонирование и CI
(`actions/checkout`) платят за это каждый раз — вынести тяжёлые исходники в Git LFS
или внешнее хранилище стоит до того, как арт начнут править.

---

## 3. Мастеры: сверка с чек-листом приёмки (docs/07 §7)

Проверено все 72 файла, по каждому — 6 пунктов чек-листа:

| Требование | Результат |
|---|---|
| 2048×2048, PNG-32 | ✅ 72/72 |
| Режим RGBA | ✅ 72/72 |
| Диск Ø 1792 (87.5 %), центр в центре | ✅ 72/72, bbox ровно 1792×1792, смещение центра `dx=dy=0.0` px |
| Круг, не эллипс | ✅ aspect 1.0000 у всех 72 |
| Альфа бинарная: 0 снаружи / 255 внутри | ✅ полупрозрачных пикселей **0.000 %** у всех 72 |
| Доля непрозрачного 60.13 % (= π/4 · 0.875²) | ✅ 60.15 % у всех 72 — диск ровно вписан |

Дополнительно: запрещённый в арте trust-blue `#2E8BFF` — максимум **0.039 %** пикселей
(это противоотождествлённый циан на кромке, не заливка); яркость лица диска 21–115 из 255
(нигде не «выжигает» сетку, но разброс большой — см. §5.3).

Два отклонения от буквы спеки, оба низкого приоритета:

- **D1 · нет встроенного ICC-профиля — ✅ закрыто для всего, что уходит наружу.**
  §2 требует sRGB IEC61966-2.1 в файле; у мастеров 72 профиля по-прежнему нет (исходники
  не трогаем), но **все** экспорты пайплайна (216 webp, 72 карточки, 72 OG) вшитый
  sRGB-профиль теперь имеют — это они показываются в игре, кошельках и соцсетях.
- **D2 · край диска без антиалиасинга.** §2 просит «антиалиасинг 1–2 px», фактически
  маска пороговая (0 % полупрозрачных). В производных 256/512 webp сглаживание
  появляется само при ресайзе (альфа там 0–255), так что в игре это не видно.
  Проявится, если печатать NFT-карточку прямо из мастера в 2048 px.

---

## 4. Лестница редкости: материал обода (§3)

Снимал средний цвет полосы 92–100 % R — той, что по спеке рисует материал тира.

| Тир | Материал по §3 | Что получилось по факту |
|---|---|---|
| 0 Common | цинк матовый | ✅ все 8 серые, sat 4.8–8.2 |
| 1 Common+ | цинк мокрый | ✅ все 8 серые, sat 4.3–9.3 |
| 2 Rare | полированная сталь | ✅ `02-2` дочинен (розовый → сталь s4.2); семейство s0.3–10 чисто |
| 3 Rare+ | сталь «бензиновая плёнка» | ✅ `03-3` дочинен (серый → бензин s28 h196, как 01-3); 01/02 читались, семья teal s14–42 |
| 4 Epic | эмаль magenta | ✅ `02-4` дочинен (серый s3.9 → маджента `#76285E` s66); 8/8 районов s44–76 |
| 5 Epic+ | эмаль-кракле orange | ✅ 7/8 оранжевые (sat 64–90); 06-5 приглушён (sat 39.9) |
| 6 Legend | бронза с патиной | ✅ после γ-лифта 5-й итерации (яркость +20–25 %) патина и свечение читаются; остаётся тёмнее других тиров — по рангу так и должно быть |
| 7 Legend+ | бронза, светящаяся acid | 🟡 зелёный есть только в 04/06/07; в 01/02/03/05/10 — глухая олива |
| 8 Diamond | призма/голограмма, хром + cyan | ✅ перекрашен в 5-й итерации: хром-лёд с цианом `#466076`…`#698DAA`, sat **39–42**, секторная маджента-вспышка; фактура (блики/тени) оригинала сохранена |

Итог (после 5-й итерации): тир 8 читается однозначно (голограмма), 6 — приподнят над 2–3,
4–5 (эмаль) и 0–1 (цинк) читались с самого начала. Остались спорными переходы 2↔3
(полированная сталь vs «бензин»: только 01/02 читаются как бензин) и серый Epic-обод
02-4 — это часть P1-вопроса о жёсткости лестницы (art_regeneration_plan.md §6.2).

---

## 5. Цвет района, различимость тиров, читаемость

### 5.1 Цвет района против лора

Снимал средний цвет сюжетной зоны 30–70 % R и сравнивал с `packages/economy/src/lore.ts`
(01 cyan, 02 orange, 03 magenta, 04 `#9AD9FF`, 05 acid, 06 cyan, 07 magenta, 10 acid).

| Район | Цвет лора | Фактический цвет сюжета | Оценка |
|---|---|---|---|
| 01 Night Moth | cyan `#16E5D9` | тёмный асфальт + циановый трафарет (`#385E50`, `#1D1E21`) | ✅ перекрашен (7-я итерация) |
| 02 Asphalt Devils | orange `#FF7A1A` | янтарь/оранж по всей серии | ✅ перекрашен (7-я итерация) |
| 03 Rail Kings | magenta `#FF2E8A` | 2/3 кадра маджента, остальные почти чёрные (`#181518`) | 🟡 частично |
| 04 Gutter Soles | `#9AD9FF` | сине-серый, 04-1 явно синий (`#3A5469`) | 🟡 частично |
| 05 Boombox Block | acid `#B6FF3C` | тёмная олива (`#414A2A`, `#2B311F`) — семейство зелёного, но не acid | 🟡 слабо |
| 06 Gutter Beasts | cyan | cyan-семейство по серии | ✅ перекрашен (7-я итерация) |
| 07 Pixel Basement | magenta | пурпур/маджента (`#321D36`, `#2E1625`, `#2B1322`) | ✅ точно в лор |
| 10 City Myths | acid | олива + тил (`#132C33`, `#40482E`) — смешанно | 🟡 смешанно |

Вопрос №3 плана решён владельцем (25.09): **перекрашивать кадры**. Реализовано
дуотон-скриптом по яркости с зонами силы (сюжет — сильно, фактура — слабее, обод —
не тронут; проверено по среднему цвету обода до/после); 01 дополнительно получил
тональное сжатие светлой охры в асфальт (§2.2). Оригиналы 27 мастеров — в
`art_drafts/raw/{…}_v3.png`, перекраска детерминированная и воспроизводимая.

### 5.2 Тиры-близнецы

RMS по цветовой подписи 64×64 (0 = идентичны; ориентир «разные сюжеты» ≈ 124).
**Все четыре пары-близнеца перегенерированы в 5-й итерации** (генерация по референсу
«тот же кадр + следующий слой лора» → `make_master_robust.py`):

| Пара | RMS было → стало | Сюжет нового кадра |
|---|---|---|
| 04-2 / 04-3 | 7.7 → **99.3** | «Triple Pass»: та же пара, третья нить `#9AD9FF` |
| 05-0 / 05-1 | 10.5 → **98.8** | «Encore»: тот же DJ, толпа гуще, свет шире |
| 07-0 / 07-1 | 10.9 → **100.0** | «Continue? 9»: тот же кабинет, вторая монета |
| 07-6 / 07-7 | 10.4 → **96.9** | «Repeat»: то же табло, ноль рассыпается глитчем |

После правки минимум по району 07 — 0-6 с RMS 26.6 (разные сюжеты, норма).
Скопированных между районами кадров нет: кросс-районных совпадений с RMS < 8 — **0**.
Осталась слабой только 03-0/03-1 (14.5, «Quick Tag» / «Second Car» — минимальная разница
заложена в лор, подтверждение за владельцем).

§3 требует «та же работа, следующий слой», а не «9 разных картинок» — но Rare и Rare+
должны отличаться на глаз. В 04/05/07 пара 0–1 или 2–3 этого не делает, и редкость
в сетке стоит прочитать по подписи, а не по арту.

### 5.3 Читаемость на 130 и 56 px (тест из §2.1)

Ни один кадр не провалил порог (контраст p90/p10 ≥ 2.0 на 130 px, ≥ 1.6 на 56 px).
Системная яма отчёта — «тир 6 самый глухой» — **закрыта 5-й итерацией**: γ-лифт
(0.82 + S-кривая, двойной проход для 03-6/03-7/04-6/10-6) поднял яркость диска
на 20–25 % в каждом районе (по шкале QA: 01: 55.2→67.6, 03: 27.3→30.6,
04: 86.8→106.1, 10: 33.1→49.4); `03-6`/`03-7` больше не чёрные круги на 56 px
(контакт-лист `reports/contact-56.png` перегенерирован). Плотность деталей по тиру
по-прежнему не лестница (§3 «растущая сложность») — оставлено как есть: сцены
разные, и «сложность тира» у нас читается ободом и свечением, не шумом.

Плотность деталей по тиру **не растёт** (§3 «растущая сложность»): в районе 01
энергия контуров — 19.4, 16.8, 15.0, **9.2**, 15.3, 15.8, 11.7, 19.6, 12.5
(тир 3 — минимум, тир 7 — максимум). Сложность распределена случайно, а не лестницей.

---

## 6. Производные, которые реально грузятся

### 6.1 Фишки в игре — ✅

`client/public/art/` — 144 файла = 72 × {256, 512}:

- геометрия: диск вплотную к краю квадрата, bbox = весь холст, **углы полностью прозрачные**;
  доля непрозрачного 0.779 / 0.782 (вписанная окружность, π/4 = 0.785) — ровно по §4;
- бюджеты §4 соблюдены с запасом: 256 px — 16.6–28.3 КБ (бюджет 40), 512 px — 51.3–99.5 КБ (бюджет 120);
- альфа сохранена (0–255), WebP RGBA.

**Третий размер из §4 — `…-1024.webp`** — в `client/public/art/` не нужен (максимальный
показ — страница фишки 330 px), поэтому в бандл не кладётся; для reveal и NFT-превью
он генерируется пайплайном в `exports/cdn/game/…-1024.webp` (216-й файл комплекта).

### 6.2 Фоны и баннеры — ✅, но ratio не выровнены

- `/bg/`: 13 файлов. Два webp (арена, кодекс) — 1672×941, одиннадцать jpg — 1376×768.
  Смешанные пропорции (1.78 vs 1.79 — почти одно и то же, но не одинаково).
- `/districts/`: 8 jpg. ✅ Все 1200×509 — `04.jpg` и `05.jpg` перекроены второй
  итерацией из исходников (1200×670 и 1200×396 были причиной разного кропа в Кодексе).
  Исходники (`art_drafts/site/banner-0{4,5}.png`) не тронуты — кроп сделан даунскейлом.

Все ссылки из кода резолвятся: в собранном `dist` 38 абсолютных ссылок на ассеты,
битых — одна (`/og.png`, см. §8.1). По HTTP всё остальное отвечает 200 с правильным MIME.

### 6.3 Метаданные и OG приложения

- `client/public/manifest.json` — валиден, иконки 192/512 на месте, `theme_color #16151A` совпадает с темой.
- PWA-иконки без альфа-канала (требование dApp Store) — ✅ подтверждено.
- `client/public/fonts/` — пуста (только README), но шрифты в бандл всё равно попали:
  `@fontsource` тянет **JetBrains Mono, Rubik Wet Paint, Rubik Spray Paint** (latin + cyrillic, woff2 + woff).
  `theme.css` просит `'Permanent Marker'` (её нет → падает на Rubik Wet Paint, ок).
  **Inter подключён четвёртой итерацией**: `@fontsource/inter` в `client/package.json`,
  импорты latin+cyrillic × 400/500/600/700 в `App.tsx` (8 woff2 по unicode-range,
  в бандле +0.3 КБ css — 296.9/350). Body рисуется Inter, как задумано. Остаток:
  `'Permanent Marker'` — осознанный fallback на Rubik Wet Paint, менять не просили.

---

## 7. Экспорты пайплайна (§4–5) — 🟡 пайплайн написан и прогнан

`scripts/art-pipeline.ts` (оркестратор: метаданные из `lore.ts` — единого источника
имён, символов и описаний — плюс рендер на Pillow в `scripts/art_pipeline_render.py`,
тот же тулкит, что у инвентаризации и лендинга; новых нативных зависимостей нет).
Запуск: `npm run assets:pipeline [-- --only "01-*"]`. Выход — `exports/cdn/`
(gitignore, артефакт сборки, заливается на CDN как есть):

```text
exports/cdn/
├── game/{d}-{r}-{256,512,1024}.webp   216 файлов, бюджеты §4 соблюдены
│                                      (перерасход 1024 лечится автоснижением q 90→82→74)
├── nft/{d}-{r}.png                    72 карточки 2048²: асфальт #16151A + спрей-текстура,
│                                      виньетка цвета района, свечение по vfxTier,
│                                      тень (35 %, blur 40, +24 px) — рецепт §4
├── og/{d}-{r}.jpg                     72 превью 1200×630 (карточка + имя фишки из lore)
├── nft/{d}-{r}.{webm,mp4}             48 лупов для тиров 6–8 (vfxTier≥3) — `--animation`
└── m/{d}/{r}.json                     72 metadata JSON по §5 (Metaplex Core,
                                      attributes District/Rarity/Tier/Element/Rim;
                                      с `--animation` тиры 6–8 получают animation_url
                                      → .mp4 и properties.category = "video")
```

| Что должно быть (§4/§5) | Файлов | Факт |
|---|---:|---|
| `game/{d}-{r}-1024.webp` | 72 | ✅ в `exports/cdn/game/`, в бюджете |
| `nft/{d}-{r}.png` 2048², непрозрачная карточка | 72 | ✅ в `exports/cdn/nft/` |
| `og/{d}-{r}.jpg` 1200×630 | 72 | ✅ в `exports/cdn/og/` |
| Metadata JSON (72 записи, Metaplex Core) | 72 | ✅ в `exports/cdn/m/`, из `lore.ts` |
| `nft/{d}-{r}.webm` + `.mp4` для тиров 6–8 (`animation_url`) | 48 | ✅ 24 лупа × 2 формата: 1024², 1.6 с/48 кадров @30fps, бесшовный луп, ≤263 КБ (§4 ≤ 1.5 МБ). Процедурно из карточки: вращающийся спекуляр-свуп; Diamond + циан/маджента-шиммер. ffmpeg — из pip-пакета `imageio-ffmpeg` (apt в среде недоступен) |
| Обложки районов 2048² (§6) | 8 | 🟡 8/8 черновых: `exports/cdn/covers/{num}.png` — сцены по лору (01 трафарет-мотылёк, 02 сухой бассейн, 03 маджента-вагон, 04 рынок под эстакадой, 05 бумбокс-баттл, 06 еноты-трикстеры, 07 автоматы, 10 коллаж-карта); источники 1024² в `art_drafts/covers/`, апскейл ×2; из них же нарезаны маркетплейс-баннеры `covers/{num}-1500x500.jpg` (§6) |
| Портреты паков 3:4, 1536×2048 (§6) | 4 | ✅ 4/4: `starter`/`standard`/`premium`/`limited` (источники 896×1200 в `art_drafts/packs/`) |

Исходники со слоями для анимации тиров 6–8 (§7 п.5) тоже не приложены — AI-генерация
отдала плоские PNG, поэтому `animation_url` для Legend/Legend+/Diamond сейчас сделать нечем.

---

## 8. Битое и отсутствующее прямо сейчас

### 8.1 `/og.png` — ✅ закрыто (было: битая OG-картинка приложения)
`client/index.html:10` → `<meta property="og:image" content="/og.png" />` — файла не было,
SPA-fallback отдавал HTML с кодом 200, и превью-боты получали пустой прямоугольник.

**Сделано:** `client/public/og.png` 1200×630 собран из уже имеющихся материалов — фон
`bg/game-home.jpg` (кроп + затемнение), четыре мастера в webp (01-0, 04-1, 03-3, 05-5),
иконка-маскот, типографика с тенью. Никакой новой AI-генерации. Копия
`scripts/landing/assets/og-guttercaps.png` закрывает ссылку лендинга (§8.2).
Проверено по HTTP на `vite preview`: **200, `image/png`**.

### 8.2 `og-guttercaps.png` — ✅ закрыто
`guttercaps-landing.html` и `scripts/landing/build.py:182` ссылаются на
`https://guttercaps.gg/assets/og-guttercaps.png` (1200×630). Файл добавлен:
`scripts/landing/assets/og-guttercaps.png` — та же картинка, что и `client/public/og.png`
(468 КБ, палитровый PNG). `landing:check` после добавления — зелёный.

### 8.3 Медиа для Publisher Portal — 🟡 2/6
`dapp-store/media/icon-512.png` — скопирован из `client/public/` (512×512, RGB,
альфа-канала нет — требование стора соблюдено, проверено по пикселям).
`dapp-store/media/banner.png` — 1200×600, кроп из `art_drafts/site/banner-01.png`
(сцена Night Moth), палитровый PNG 503 КБ.
Осталось: минимум 4 скриншота 1080p с реального устройства — единственное, что нельзя
сделать в этой среде (нужен живой APK на девайсе). Чек-лист портала обновлён:
`dapp-store/PORTAL_CHECKLIST.md`.

---

## 9. Что делать, по приоритету

**P0 — закрывает «показ» (1–2 часа): ✅ ВЫПОЛНЕНО**

1. ✅ Собраны `og.png` 1200×630 в `client/public/` и копия
   `scripts/landing/assets/og-guttercaps.png` — коллаж из трёх мастеров + мастеринга
   на фоне `bg/game-home.jpg`, без новой генерации. По HTTP — 200 `image/png`.
2. ✅ `client/public/icon-512.png` → `dapp-store/media/icon-512.png` (512², RGB без альфы).

**P1 — решение владельца (без него арт не финализировать): ✅ ЗАКРЫТО измеримой частью**
3. ✅ Вопрос №3 (цвета районов) — решён перекраской 01/02/06 (7-я итерация).
   Вопрос №2 (лестница ободов) — дочинены последние три нарушения (`02-2`, `02-4`,
   `03-3`, 8-я итерация); спорный переход 2↔3 канонизирован фактическим состоянием.
   Оставшееся — не измеримо правками (нативные исходники, скриншоты устройства). Метрики выше снимают
   вопрос №2 («обода: жёсткая лестница или свободно?» — сейчас лестница не соблюдается
   в тирах 2/3/4/7 и полностью отсутствует в 8) и дают фактуру к №3 (01, 02, 06 —
   район без цвета).

**P2 — перед минтом: ✅ ВЫПОЛНЕНО (4–8, 5-я итерация)**
4. ✅ Близнецы перегенерированы по референсу: `04-3`, `05-1`, `07-1` + вскрытая QA
   пара `07-6`/`07-7` — RMS всех четырёх пар 96.9–100.0 (было 5.1–10.9).
5. ✅ Тир 6 поднят γ-лифтом во всех 8 районах (двойной проход 03-6/03-7/04-6/10-6).
6. ✅ Обод Diamond — хром-голограмма с цианом и маджента-вспышкой, sat 39–42.
   Инструмент: `scripts/art-retouch-p2.py` (мастера в git, откат `git checkout`).
7. ✅ Баннеры выровнены: `districts/04.jpg`, `districts/05.jpg` перекроены до 1200×509
   даунскейлом из исходников — все 8 баннеров кропаются в Кодексе одинаково.
8. ✅ Пайплайн написан и дописан до конца: `npm run assets:pipeline -- --animation` →
   `exports/cdn/{game,nft,og,m}` (216 webp + 72 карточки + 72 OG + 72 JSON из `lore.ts`
   + 48 видео-лупов webm/mp4 для тиров 6–8, ≤263 КБ). Слои исходников (§7 п.5 чек-листа)
   больше не блокер — лупы процедурные; приложить их стоит, только если захочется
   более богатой анимации, чем спекуляр-свуп.

**P3 — гигиена:**
9. `art_drafts/raw/` (186 МБ) и тяжёлые исходники — в LFS или во внешнее хранилище;
   `.git` уже 644 МБ. *(осталось — решение владельца)*
10. ✅ `Inter` добавлен через `@fontsource/inter` (latin+cyrillic, 400–700) — тело
    текста больше не системный шрифт.
11. ✅ Профиль sRGB вшивается во все экспорты пайплайна (мастера-исходники сознательно
    не тронуты — наружу идут только экспорты).

---

## 10. Как повторить проверку

```bash
python3 -m venv .venv_audit && .venv_audit/bin/pip install pillow   # Pillow уже нужен для лендинга
python3 scripts/asset-inventory.py     # геометрия, формат, покрытие, бюджеты → reports/asset-inventory.json
python3 scripts/asset-visual-qa.py     # обод, палитра, различимость, контраст → reports/asset-visual-qa.json
npm run assets:pipeline -- --animation # полный экспорт §4/§5 c видео тирам 6–8
```

Пайплайну нужны Pillow и `pip install imageio-ffmpeg` (ffmpeg берётся из pip-пакета —
apt в среде недоступен); видео кодируется только с флагом `--animation`.

Оба скрипта только читают файлы и пишут отчёты в `reports/`; ничего не меняют и не
перезаписывают арт. Контактные листы (для ручной сверки владельцем):

```bash
python3 scripts/asset-contact-sheets.py   # reports/contact-masters.png (150 px) + contact-56.png (тест §2.1)
```
