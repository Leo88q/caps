{
  "name": "guttercaps-workspace",
  "private": true,
  "scripts": {
    "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/localnet/**/*.ts",
    "verify": "npm run economy:check && npm run economy:test && npm --prefix client run typecheck && npm --prefix client test && npm --prefix client run build && npm --prefix backend run typecheck && npm run backend:test && npm run landing:check",
    "setup": "ts-node scripts/setup.ts",
    "skr-pool": "ts-node scripts/skr-pool.ts",
    "economy:report": "npm --prefix packages/economy run report",
    "economy:test": "npm --prefix packages/economy test",
    "economy:golden": "npm --prefix packages/economy run golden",
    "backend:dev": "npm --prefix backend run dev",
    "backend:backfill": "npm --prefix backend run backfill",
    "backend:test": "npm --prefix backend test",
    "landing:build": "python3 scripts/landing/build.py",
    "landing:check": "node --experimental-strip-types scripts/landing/check.ts && node scripts/landing/smoke.mjs",
    "economy:check": "npm --prefix packages/economy run check && npm --prefix packages/economy run golden && node --experimental-strip-types packages/economy/scripts/sync-check.ts",
    "programs:test": "cargo test --workspace --exclude chip-game",
    "programs:build": "anchor build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.8",
    "@solana/web3.js": "^1.95.3"
  },
  "devDependencies": {
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.7",
    "@types/node": "^22.1.0",
    "chai": "^4.4.1",
    "mocha": "^10.7.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "workspaces": [
    "packages/*",
    "client",
    "backend"
  ],
  "overrides": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1"
  }
}
