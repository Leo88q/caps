# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Anchor / Rust
target/
.anchor/
test-ledger/
**/*.so

# Backend
backend/.env

# Ops — Pyth pusher secrets (Hermes key, RPC key) and the payer hot wallet
ops/pyth-pusher/.env
ops/pyth-pusher/payer.json
backend/prisma/migrations/dev/
*.sqlite
*.db

# Generated (regenerate with `npm run economy:golden`; kept in git on purpose — it is a test fixture)
!packages/economy/golden/pack_expand.json

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Python
__pycache__/
*.pyc

**/.DS_Store

# ── Secrets: by convention, not by luck (docs/09 §1.4, docs/07 §1.1) ─────────────────────────
# `backend/.env` was the only env file ignored, so a `client/.env.local` with a DSN or a key, or a
# `backend/.env.prod`, was one `git add -A` away from the public repo. Same for the cold keypairs
# `npm run program-ids -- new` writes: the ceremony is worthless if step 2 is "commit them by habit".
.env
.env.*
!**/.env.example
!**/.env.test
*-keypair.json
!tests/localnet/fixtures/sb_mock-keypair.json
keys/
*.pem
*.p12
# id_ed25519-style names sometimes land in a repo root during a manual deploy
.deploy*/

# Playwright / Lighthouse artifacts (test-results/lighthouse is the lhci report)
test-results/
playwright-report/
/k6-lt1.json
/auth/

# local audit venv (see reports/ASSET-INVENTORY-2026-09-25.md §10)
.venv_audit/
