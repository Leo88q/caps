# GUTTERCAPS — Pyth price_pusher feed configuration (owner decision Q7: own pusher).
# Numbers mirror packages/economy/src/oracle.ts::PYTH_PUSHER and are pinned by
# `npm run economy:check` (sync-check). Change them there first.
#
# Trigger = ANY condition per feed; when one feed triggers, both are pushed in the same batch.
# Worst-case on-chain age ≈ time_difference (30) + pushing-frequency (10) + landing (≈5) = 45 s,
# comfortably inside chip_core's SOL_PRICE_MAX_AGE_SECS = 60.
- alias: SOL/USD
  id: ef0d8b6fda2ceba41da15d4095d1da392a0d2f8ed0c6c7bc0f4cfac8c280b56d
  time_difference: 30
  price_deviation: 0.5
  confidence_ratio: 50
- alias: SKR/USD
  id: 38846ec4d0dbe808091817f5c0d6ab8058e25422348ddf97db52b6c378a93bf9
  time_difference: 30
  price_deviation: 0.5
  confidence_ratio: 50
