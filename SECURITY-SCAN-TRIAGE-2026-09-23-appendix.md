# Приложение: 188 находок Watchtower (`reports/guttercaps-audit.json`, коммит `1aea14c`) — вердикт по каждой

Скан выполнялся по ревизии ≈ `cda1747`; номера строк — оттуда (`git show cda1747:programs/<path>`). Вердикты: **REAL** — подтверждено и исправлено в этой ветке; **FALSE** — паттерн есть, эксплуатации нет (доказательство в последней колонке); **MOCK** — `programs/sb_mock`, тестовая заглушка, не деплоится.

## Сводка по правилам

| правило | всего | REAL | FALSE | MOCK |
|---|---:|---:|---:|---:|
| SW002 | 70 | 0 | 57 | 13 |
| SW003 | 1 | 0 | 0 | 1 |
| SW009 | 1 | 0 | 1 | 0 |
| SW010 | 2 | 0 | 2 | 0 |
| SW013 | 54 | 0 | 54 | 0 |
| SW016 | 12 | 0 | 12 | 0 |
| SW022 | 1 | 0 | 0 | 1 |
| SW023 | 13 | 0 | 13 | 0 |
| SW024 | 20 | 0 | 20 | 0 |
| SW025 | 3 | 0 | 0 | 3 |
| SW026 | 1 | 0 | 1 | 0 |
| SW027 | 10 | 6 | 3 | 1 |
| **итого** | **188** | **6** | **163** | **19** |

## Все находки

| # | правило | sev | место | аккаунт / функция | вердикт | почему |
|---:|---|---|---|---|---|---|
| 1 | SW027 | low | `arena/src/lib.rs:415` | `set_pauser_handler` | REAL | G-05 (Low), arena — то же, `PauserChanged` |
| 2 | SW002 | critical | `arena/src/lib.rs:466` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 3 | SW002 | critical | `arena/src/lib.rs:468` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 4 | SW002 | critical | `arena/src/lib.rs:505` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 5 | SW002 | critical | `arena/src/lib.rs:507` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 6 | SW023 | critical | `arena/src/lib.rs:558` | `create_battle_v2_handler` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 7 | SW023 | critical | `arena/src/lib.rs:613` | `create_battle_handler` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 8 | SW013 | high | `arena/src/lib.rs:657` | `randomness` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 9 | SW013 | high | `arena/src/lib.rs:660` | `rng_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 10 | SW002 | critical | `arena/src/lib.rs:663` | `reward_escrow` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 11 | SW002 | critical | `arena/src/lib.rs:666` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 12 | SW002 | critical | `arena/src/lib.rs:671` | `lut_signer` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 13 | SW002 | critical | `arena/src/lib.rs:673` | `lut` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 14 | SW002 | critical | `arena/src/lib.rs:735` | `randomness` | FALSE | `randomness` пиннится состоянием (`constraint = pending.randomness == randomness.key()` / `battle.randomness == …`) либо проверяется в хелпере `randomness::parse_checked`/`reveal_owned` (owner = программа, authority = `["rng_auth"]`). В vouchers.rs — создаётся и owner-проверяется внутри CPI в chip_core |
| 15 | SW013 | high | `arena/src/lib.rs:738` | `rng_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 16 | SW002 | critical | `arena/src/lib.rs:741` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 17 | SW002 | critical | `arena/src/lib.rs:743` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 18 | SW002 | critical | `arena/src/lib.rs:745` | `stats` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 19 | SW002 | critical | `arena/src/lib.rs:748` | `reward_escrow` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 20 | SW002 | critical | `arena/src/lib.rs:805` | `challenger` | FALSE | `CloseBattleRandomness.challenger` — то же: seeds `["rng", 2, challenger, nonce]` + `battle.randomness == randomness.key()` |
| 21 | SW013 | high | `arena/src/lib.rs:808` | `randomness` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 22 | SW013 | high | `arena/src/lib.rs:820` | `battle` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 23 | SW002 | critical | `arena/src/lib.rs:827` | `reward_escrow` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 24 | SW002 | critical | `arena/src/lib.rs:832` | `lut` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 25 | SW002 | critical | `arena/src/lib.rs:835` | `lut_signer` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 26 | SW023 | critical | `arena/src/lib.rs:945` | `accept_battle_v2_handler` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 27 | SW023 | critical | `arena/src/lib.rs:982` | `accept_battle_handler` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 28 | SW010 | critical | `arena/src/lib.rs:1021` | `winner_cg` | FALSE | `token::mint = config.cg_mint` в структуре + `require_keys_eq!(winner_cg.owner, winner)` в хендлере, где `winner ∈ {challenger, opponent}` |
| 29 | SW024 | high | `chip_core/src/economy.rs:284` | `top_mass` | FALSE | `economy.rs:276` `if top_mass == 0 { return … }` перед делением |
| 30 | SW024 | high | `chip_core/src/economy.rs:309` | `RANGE` | FALSE | локальная константа `RANGE` > 0 |
| 31 | SW024 | high | `chip_core/src/economy.rs:314` | `RANGE` | FALSE | локальная константа `RANGE` > 0 |
| 32 | SW024 | high | `chip_core/src/economy.rs:320` | `RANGE` | FALSE | локальная константа `RANGE` > 0 |
| 33 | SW024 | high | `chip_core/src/economy.rs:353` | `pool . len () . max (1)` | FALSE | делитель `.max(1)` ≥ 1 по построению |
| 34 | SW013 | high | `chip_core/src/instructions/admin.rs:31` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 35 | SW027 | low | `chip_core/src/instructions/admin.rs:113` | `create_collection` | REAL | G-05 (Low): `emit!(CollectionCreated{by,idx,core_collection})` |
| 36 | SW002 | critical | `chip_core/src/instructions/admin.rs:307` | `tree_config` | FALSE | `RegisterTree.tree_config`: реестр только связывает ключ (`merkle_tree`/`tree_config`) с индексом коллекции; owner и раскладка проверяются Bubblegum при каждом mint/replace-leaf CPI, байты дерева chip_core не парсит |
| 37 | SW027 | low | `chip_core/src/instructions/admin.rs:491` | `set_pauser` | REAL | G-05 (Low): ротация pauser не давала события — бэкенд и алерты слепы. Исправлено: `emit!(PauserChanged{by,pauser})` (chip_core/staking/arena) → `authority_changes`, `/v1/admin/params.authorityHistory`, `AuthorityChangeIndexed` |
| 38 | SW027 | low | `chip_core/src/instructions/admin.rs:519` | `propose_admin` | REAL | G-05 (Low): `emit!(AdminProposed{by,new_admin})` (+ `AdminAccepted` в `accept_admin`); плюс gauge `admin_transfer_pending` с чейна и алерт `AdminTransferProposed` |
| 39 | SW013 | high | `chip_core/src/instructions/admin.rs:552` | `items` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 40 | SW016 | high | `chip_core/src/instructions/admin.rs:552` | `items` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 41 | SW013 | high | `chip_core/src/instructions/chip.rs:43` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 42 | SW013 | high | `chip_core/src/instructions/chip.rs:126` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 43 | SW013 | high | `chip_core/src/instructions/chip.rs:197` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 44 | SW002 | critical | `chip_core/src/instructions/chip.rs:204` | `new_owner` | FALSE | `DeliverSold.new_owner` — данные, а не авторитет: инструкция CPI-only (`caller` = `["market_auth"]` PDA маркета, `NotProgramCaller`), покупателя выбрал маркет |
| 45 | SW013 | high | `chip_core/src/instructions/chip.rs:272` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 46 | SW027 | low | `chip_core/src/instructions/compressed.rs:83` | `set_compressed_claim_listed` | FALSE | CPI-only переход (`caller` = `["market_auth"]`); событие эмитит вызывающая программа: market `CompressedClaimListed`/`ListingCancelled` |
| 47 | SW027 | low | `chip_core/src/instructions/compressed.rs:137` | `transfer_compressed_claim` | FALSE | CPI-only; market эмитит `CompressedClaimSold`/`CompressedAssetSold` |
| 48 | SW027 | low | `chip_core/src/instructions/compressed.rs:183` | `set_compressed_claim_staked` | FALSE | CPI-only (`["stake_auth"]`); staking эмитит `Staked`/`Unstaked` |
| 49 | SW002 | critical | `chip_core/src/instructions/compressed.rs:302` | `randomness` | FALSE | `randomness` пиннится состоянием (`constraint = pending.randomness == randomness.key()` / `battle.randomness == …`) либо проверяется в хелпере `randomness::parse_checked`/`reveal_owned` (owner = программа, authority = `["rng_auth"]`). В vouchers.rs — создаётся и owner-проверяется внутри CPI в chip_core |
| 50 | SW016 | high | `chip_core/src/instructions/compressed.rs:306` | `settlement` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 51 | SW023 | critical | `chip_core/src/instructions/compressed.rs:488` | `open_compressed_pack` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 52 | SW027 | low | `chip_core/src/instructions/compressed.rs:592` | `fuse_compressed_claims` | REAL | G-04 (Low): сплав claim-путём не давал события — квесты `d_fuse1`/`p_first_fusion`, лента активности и WS не видели его. Исправлено: `emit!(CompressedClaimsFused{…})` → `fusions` |
| 53 | SW023 | critical | `chip_core/src/instructions/compressed.rs:653` | `fuse_compressed_claims` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 54 | SW013 | high | `chip_core/src/instructions/compressed.rs:808` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 55 | SW024 | high | `chip_core/src/instructions/compressed.rs:952` | `BPS_DENOM as u64` | FALSE | константа `BPS_DENOM = 10_000` |
| 56 | SW002 | critical | `chip_core/src/instructions/compressed.rs:1260` | `settlement` | FALSE | `CancelCompressedClaim.settlement`: хендлер `require_keys_eq!(settlement.key(), claim.settlement)` при ненулевом `claim.settlement`, плюс `is_writable` и owner-проверка перед `Account::try_from`; при нулевом — не трогается |
| 57 | SW016 | high | `chip_core/src/instructions/fusion.rs:72` | `items` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 58 | SW013 | high | `chip_core/src/instructions/fusion.rs:88` | `result_state` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 59 | SW013 | high | `chip_core/src/instructions/fusion.rs:96` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 60 | SW023 | critical | `chip_core/src/instructions/fusion.rs:384` | `fuse` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 61 | SW002 | critical | `chip_core/src/instructions/fusion.rs:632` | `randomness` | FALSE | `randomness` пиннится состоянием (`constraint = pending.randomness == randomness.key()` / `battle.randomness == …`) либо проверяется в хелпере `randomness::parse_checked`/`reveal_owned` (owner = программа, authority = `["rng_auth"]`). В vouchers.rs — создаётся и owner-проверяется внутри CPI в chip_core |
| 62 | SW002 | critical | `chip_core/src/instructions/fusion.rs:642` | `result_asset` | FALSE | `FuseReveal.result_asset`: `mint_result` выводит `["asset", pending, 0, 0]` (`find_program_address` + `require_keys_eq`) и создаёт аккаунт `invoke_signed` — существующий/чужой аккаунт = ошибка create |
| 63 | SW002 | critical | `chip_core/src/instructions/fusion.rs:645` | `result_state` | FALSE | `FuseReveal.result_state`: там же — `["chip", result_asset]` + `system_program::create_account` под подписью PDA |
| 64 | SW013 | high | `chip_core/src/instructions/fusion.rs:652` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 65 | SW023 | critical | `chip_core/src/instructions/fusion.rs:789` | `fuse_reveal` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 66 | SW013 | high | `chip_core/src/instructions/fusion.rs:855` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 67 | SW023 | critical | `chip_core/src/instructions/fusion.rs:880` | `cancel_stale_fusion` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 68 | SW024 | high | `chip_core/src/instructions/packs.rs:89` | `(price as u128)` | FALSE | `packs.rs:86` `require!(price > 0)` перед делением |
| 69 | SW016 | high | `chip_core/src/instructions/packs.rs:121` | `pity` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 70 | SW002 | critical | `chip_core/src/instructions/packs.rs:147` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 71 | SW002 | critical | `chip_core/src/instructions/packs.rs:149` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 72 | SW013 | high | `chip_core/src/instructions/packs.rs:156` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 73 | SW024 | high | `chip_core/src/instructions/packs.rs:261` | `BPS_DENOM as u64` | FALSE | константа `BPS_DENOM = 10_000` |
| 74 | SW024 | high | `chip_core/src/instructions/packs.rs:343` | `BPS_DENOM as u64` | FALSE | константа `BPS_DENOM = 10_000` |
| 75 | SW016 | high | `chip_core/src/instructions/packs.rs:434` | `pity` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 76 | SW002 | critical | `chip_core/src/instructions/packs.rs:459` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 77 | SW002 | critical | `chip_core/src/instructions/packs.rs:461` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 78 | SW002 | critical | `chip_core/src/instructions/packs.rs:582` | `randomness` | FALSE | `randomness` пиннится состоянием (`constraint = pending.randomness == randomness.key()` / `battle.randomness == …`) либо проверяется в хелпере `randomness::parse_checked`/`reveal_owned` (owner = программа, authority = `["rng_auth"]`). В vouchers.rs — создаётся и owner-проверяется внутри CPI в chip_core |
| 79 | SW013 | high | `chip_core/src/instructions/packs.rs:593` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 80 | SW023 | critical | `chip_core/src/instructions/packs.rs:778` | `open_pack` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 81 | SW024 | high | `chip_core/src/instructions/packs.rs:869` | `BPS_DENOM as u64` | FALSE | константа `BPS_DENOM = 10_000` |
| 82 | SW013 | high | `chip_core/src/instructions/packs.rs:958` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 83 | SW013 | high | `chip_core/src/instructions/packs.rs:1060` | `vault` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 84 | SW002 | critical | `chip_core/src/instructions/packs.rs:1063` | `treasury` | FALSE | `has_one = treasury` на `config` в той же структуре (`PayService`, `SweepVault`, market `Buy`, `AcceptOffer`) — ключ берётся из `GameConfig`, не от вызывающего |
| 85 | SW023 | critical | `chip_core/src/instructions/packs.rs:1093` | `sweep_vault` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 86 | SW002 | critical | `chip_core/src/instructions/rng.rs:49` | `reward_escrow` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 87 | SW002 | critical | `chip_core/src/instructions/rng.rs:52` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 88 | SW002 | critical | `chip_core/src/instructions/rng.rs:57` | `lut_signer` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 89 | SW002 | critical | `chip_core/src/instructions/rng.rs:59` | `lut` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 90 | SW002 | critical | `chip_core/src/instructions/rng.rs:125` | `randomness` | FALSE | `randomness` пиннится состоянием (`constraint = pending.randomness == randomness.key()` / `battle.randomness == …`) либо проверяется в хелпере `randomness::parse_checked`/`reveal_owned` (owner = программа, authority = `["rng_auth"]`). В vouchers.rs — создаётся и owner-проверяется внутри CPI в chip_core |
| 91 | SW002 | critical | `chip_core/src/instructions/rng.rs:131` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 92 | SW002 | critical | `chip_core/src/instructions/rng.rs:133` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 93 | SW002 | critical | `chip_core/src/instructions/rng.rs:135` | `stats` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 94 | SW002 | critical | `chip_core/src/instructions/rng.rs:138` | `reward_escrow` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 95 | SW002 | critical | `chip_core/src/instructions/rng.rs:194` | `owner` | FALSE | `CloseRandomness.owner` входит в seeds randomness-PDA `["rng", kind, owner, nonce]` (`close_owned` → `find_program_address` + `require_keys_eq`), подмена = другой аккаунт, которого нет; rent всегда уходит именно `owner` |
| 96 | SW013 | high | `chip_core/src/instructions/rng.rs:197` | `randomness` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 97 | SW002 | critical | `chip_core/src/instructions/rng.rs:209` | `pending` | FALSE | chip_core `CloseRandomness.pending`: хендлер выводит `["pending"|"fusion", owner, nonce]` через `find_program_address`, `require_keys_eq`, и требует `data_is_empty() && lamports() == 0` (закрыт). staking `OpenVoucher.pending`: `init` внутри CPI в chip_core (seeds там) |
| 98 | SW002 | critical | `chip_core/src/instructions/rng.rs:211` | `reward_escrow` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 99 | SW002 | critical | `chip_core/src/instructions/rng.rs:216` | `lut` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 100 | SW002 | critical | `chip_core/src/instructions/rng.rs:219` | `lut_signer` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 101 | SW016 | high | `chip_core/src/instructions/services.rs:50` | `ledger` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 102 | SW016 | high | `chip_core/src/instructions/services.rs:56` | `items` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 103 | SW002 | critical | `chip_core/src/instructions/services.rs:60` | `treasury` | FALSE | `has_one = treasury` на `config` в той же структуре (`PayService`, `SweepVault`, market `Buy`, `AcceptOffer`) — ключ берётся из `GameConfig`, не от вызывающего |
| 104 | SW026 | high | `chip_core/src/state.rs:140` | `?` | FALSE | `VaultLedger::totals` (`state.rs:140`): `Account::try_from` (owner + discriminator) → `l.shard == i` → `create_program_address([SEED, i, l.bump])` → `require_keys_eq!(exp, ai.key())`. Bump берётся из аккаунта, который уже прошёл owner-проверку; неверный bump даёт `Err`/другой адрес, и `require_keys_eq` падает |
| 105 | SW013 | high | `market/src/lib.rs:256` | `listing` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 106 | SW013 | high | `market/src/lib.rs:258` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 107 | SW013 | high | `market/src/lib.rs:265` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 108 | SW013 | high | `market/src/lib.rs:372` | `listing` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 109 | SW013 | high | `market/src/lib.rs:374` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 110 | SW013 | high | `market/src/lib.rs:380` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 111 | SW013 | high | `market/src/lib.rs:438` | `listing` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 112 | SW013 | high | `market/src/lib.rs:440` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 113 | SW013 | high | `market/src/lib.rs:447` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 114 | SW002 | critical | `market/src/lib.rs:456` | `treasury` | FALSE | `has_one = treasury` на `config` в той же структуре (`PayService`, `SweepVault`, market `Buy`, `AcceptOffer`) — ключ берётся из `GameConfig`, не от вызывающего |
| 115 | SW002 | critical | `market/src/lib.rs:459` | `buyback_wallet` | FALSE | `has_one = buyback_wallet` на `config` (market `Buy`/`AcceptOffer`) |
| 116 | SW013 | high | `market/src/lib.rs:613` | `offer` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 117 | SW009 | high | `market/src/lib.rs:668` | `bidder_usdc` | FALSE | `CancelOffer.bidder_usdc`: `token::authority = bidder` (Signer) и escrow `associated_token::mint = bidder_usdc.mint` — при чужом mint transfer из USDC-escrow падает в SPL (mint mismatch); ущерб невозможен, самому себе можно лишь сломать свой cancel (Low foot-gun, задокументировано) |
| 118 | SW013 | high | `market/src/lib.rs:714` | `offer` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 119 | SW013 | high | `market/src/lib.rs:718` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 120 | SW013 | high | `market/src/lib.rs:724` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 121 | SW002 | critical | `market/src/lib.rs:733` | `treasury` | FALSE | `has_one = treasury` на `config` в той же структуре (`PayService`, `SweepVault`, market `Buy`, `AcceptOffer`) — ключ берётся из `GameConfig`, не от вызывающего |
| 122 | SW002 | critical | `market/src/lib.rs:735` | `buyback_wallet` | FALSE | `has_one = buyback_wallet` на `config` (market `Buy`/`AcceptOffer`) |
| 123 | SW013 | high | `market/src/lib.rs:932` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 124 | SW013 | high | `market/src/lib.rs:998` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 125 | SW013 | high | `market/src/lib.rs:1062` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 126 | SW013 | high | `market/src/lib.rs:1184` | `listing` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 127 | SW013 | high | `market/src/lib.rs:1195` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 128 | SW013 | high | `market/src/lib.rs:1206` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 129 | SW013 | high | `market/src/lib.rs:1291` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 130 | SW013 | high | `market/src/lib.rs:1371` | `market_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 131 | SW023 | critical | `market/src/lib.rs:1459` | `buy_compressed_asset_handler` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 132 | SW025 | medium | `sb_mock/src/lib.rs:95` | `.unwrap()` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 133 | SW025 | medium | `sb_mock/src/lib.rs:101` | `.unwrap()` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 134 | SW002 | critical | `sb_mock/src/lib.rs:135` | `reward_escrow` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 135 | SW002 | critical | `sb_mock/src/lib.rs:150` | `wrapped_sol_mint` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 136 | SW002 | critical | `sb_mock/src/lib.rs:154` | `lut_signer` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 137 | SW002 | critical | `sb_mock/src/lib.rs:156` | `lut` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 138 | SW002 | critical | `sb_mock/src/lib.rs:173` | `recent_slothashes` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 139 | SW002 | critical | `sb_mock/src/lib.rs:187` | `stats` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 140 | SW002 | critical | `sb_mock/src/lib.rs:193` | `recent_slothashes` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 141 | SW002 | critical | `sb_mock/src/lib.rs:196` | `reward_escrow` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 142 | SW002 | critical | `sb_mock/src/lib.rs:201` | `wrapped_sol_mint` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 143 | SW002 | critical | `sb_mock/src/lib.rs:212` | `reward_escrow` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 144 | SW002 | critical | `sb_mock/src/lib.rs:223` | `wrapped_sol_mint` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 145 | SW002 | critical | `sb_mock/src/lib.rs:225` | `lut` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 146 | SW002 | critical | `sb_mock/src/lib.rs:228` | `lut_signer` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 147 | SW003 | critical | `sb_mock/src/lib.rs:259` | `invoke_signed` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 148 | SW027 | low | `sb_mock/src/lib.rs:326` | `randomness_close` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 149 | SW022 | high | `sb_mock/src/lib.rs:332` | `randomness_close` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 150 | SW025 | medium | `sb_mock/src/lib.rs:367` | `.unwrap()` | MOCK | `programs/sb_mock` — локальная заглушка Switchboard для LiteSVM-тестов; в devnet/mainnet-артефакт не входит (`SB_PROGRAM_ID` пинится на настоящий Switchboard, `verify-deploy artifact` сканирует бинарь на чужие пины) |
| 151 | SW027 | low | `staking/src/instructions/emission.rs:142` | `set_pauser` | REAL | G-05 (Low): ротация pauser не давала события — бэкенд и алерты слепы. Исправлено: `emit!(PauserChanged{by,pauser})` (chip_core/staking/arena) → `authority_changes`, `/v1/admin/params.authorityHistory`, `AuthorityChangeIndexed` |
| 152 | SW024 | high | `staking/src/instructions/emission.rs:212` | `DAY` | FALSE | константа `DAY = 86_400`; знак/переполнение `now - genesis_ts` — это G-01 (исправлено ранее, `BeforeGenesis`) |
| 153 | SW024 | high | `staking/src/instructions/emission.rs:241` | `DAY as u64` | FALSE | константа `DAY` |
| 154 | SW024 | high | `staking/src/instructions/emission.rs:243` | `DAY as u64` | FALSE | константа `DAY` |
| 155 | SW013 | high | `staking/src/instructions/items.rs:121` | `rewarder` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 156 | SW002 | critical | `staking/src/instructions/items.rs:126` | `items` | FALSE | staking `ClaimItemRoot.items`: chip_core `["items", wallet]`, `init_if_needed` внутри CPI `grant_booster` (payer = wallet); при повторном входе chip_core сверяет `items.owner` |
| 157 | SW010 | critical | `staking/src/instructions/skr.rs:171` | `to` | FALSE | `WithdrawSkr`: `has_one = admin` (Signer) на `emission`, `token::mint = pool.skr_mint`; получатель по замыслу произвольный ATA админа, сумма ≤ `budget` (незарезервированный остаток) |
| 158 | SW016 | high | `staking/src/instructions/stake.rs:37` | `stake` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 159 | SW024 | high | `staking/src/instructions/stake.rs:102` | `ACC_PRECISION` | FALSE | константа `ACC_PRECISION = 1e12` |
| 160 | SW024 | high | `staking/src/instructions/stake.rs:197` | `ACC_PRECISION` | FALSE | константа `ACC_PRECISION = 1e12` |
| 161 | SW013 | high | `staking/src/instructions/stake.rs:220` | `cstake` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 162 | SW016 | high | `staking/src/instructions/stake.rs:222` | `set_bonus` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 163 | SW013 | high | `staking/src/instructions/stake.rs:224` | `stake_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 164 | SW013 | high | `staking/src/instructions/stake.rs:230` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 165 | SW024 | high | `staking/src/instructions/stake.rs:303` | `ACC_PRECISION` | FALSE | константа `ACC_PRECISION = 1e12` |
| 166 | SW013 | high | `staking/src/instructions/stake.rs:329` | `cstake` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 167 | SW013 | high | `staking/src/instructions/stake.rs:331` | `stake_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 168 | SW013 | high | `staking/src/instructions/stake.rs:337` | `chip` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 169 | SW016 | high | `staking/src/instructions/stake.rs:421` | `set_bonus` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 170 | SW013 | high | `staking/src/instructions/stake.rs:423` | `stake_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 171 | SW024 | high | `staking/src/instructions/stake.rs:484` | `ACC_PRECISION` | FALSE | константа `ACC_PRECISION = 1e12` |
| 172 | SW016 | high | `staking/src/instructions/stake.rs:518` | `set_bonus` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 173 | SW013 | high | `staking/src/instructions/stake.rs:520` | `stake_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 174 | SW023 | critical | `staking/src/instructions/stake.rs:587` | `stake_compressed_chip_v2` | FALSE | `remaining_accounts` типизируются через `Account::try_from` (owner + discriminator) и сверяются с `find_program_address` / полями состояния (`["chip", asset]`, `["collection", idx]`, `VaultLedger` — ровно `LEDGER_SHARDS` шардов по порядку, claim.buyer == owner) до любого использования; в CPI уходят только после проверки |
| 175 | SW024 | high | `staking/src/instructions/stake.rs:605` | `ACC_PRECISION` | FALSE | константа `ACC_PRECISION = 1e12` |
| 176 | SW013 | high | `staking/src/instructions/stake.rs:633` | `stake_auth` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 177 | SW024 | high | `staking/src/instructions/stake.rs:740` | `ACC_PRECISION` | FALSE | константа `ACC_PRECISION = 1e12` |
| 178 | SW013 | high | `staking/src/instructions/stake.rs:762` | `set_bonus` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 179 | SW016 | high | `staking/src/instructions/stake.rs:762` | `set_bonus` | FALSE | PDA по `owner`/`buyer`/`wallet`; при повторном входе владелец сверяется (`items.owner`, `ledger`-шард по seeds, `pity.owner`, `set_bonus.owner`), `StakeCg` на существующем стейке добавляет, а не переинициализирует; `settlement` в `OpenCompressedPack` — `["compressed_settlement", pending.buyer, nonce]`, buyer берётся из `PendingPack` |
| 180 | SW013 | high | `staking/src/instructions/vouchers.rs:117` | `rewarder` | FALSE | Детектор помечает любой `seeds = […key()…]`. Здесь seeds либо константные PDA без внешнего ключа (`market_auth`, `stake_auth`, `rng_auth`, `rewarder`, `vault`, `set_bonus`), либо ключ берётся из уже связанного аккаунта (`asset` с `has_one`/`address`, `listing.asset`, `pending`, `owner: Signer`) — стандартный Anchor-паттерн; часть строк ссылается сама на себя (артефакт парсера) |
| 181 | SW002 | critical | `staking/src/instructions/vouchers.rs:122` | `pity` | FALSE | staking `OpenVoucher.pity`: chip_core `["pity", wallet]` с `init_if_needed` внутри CPI — seeds проверяет chip_core |
| 182 | SW002 | critical | `staking/src/instructions/vouchers.rs:125` | `pending` | FALSE | chip_core `CloseRandomness.pending`: хендлер выводит `["pending"|"fusion", owner, nonce]` через `find_program_address`, `require_keys_eq`, и требует `data_is_empty() && lamports() == 0` (закрыт). staking `OpenVoucher.pending`: `init` внутри CPI в chip_core (seeds там) |
| 183 | SW002 | critical | `staking/src/instructions/vouchers.rs:128` | `randomness` | FALSE | `randomness` пиннится состоянием (`constraint = pending.randomness == randomness.key()` / `battle.randomness == …`) либо проверяется в хелпере `randomness::parse_checked`/`reveal_owned` (owner = программа, authority = `["rng_auth"]`). В vouchers.rs — создаётся и owner-проверяется внутри CPI в chip_core |
| 184 | SW002 | critical | `staking/src/instructions/vouchers.rs:132` | `rng_auth` | FALSE | staking `OpenVoucher.rng_auth`: `["rng_auth"]` chip_core, выводится внутри CPI (`seeds = [RNG_AUTH_SEED], bump`) |
| 185 | SW002 | critical | `staking/src/instructions/vouchers.rs:136` | `queue` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 186 | SW002 | critical | `staking/src/instructions/vouchers.rs:138` | `oracle` | FALSE | Switchboard On-Demand обвязка: аккаунт передаётся в CPI Switchboard, который сам проверяет owner/PDA-связь (`queue`/`oracle`/`stats`/`reward_escrow`/`lut`/`lut_signer`/`program_state`); наш код из него не читает и ему не платит. `queue` дополнительно сверяется с пином `SB_QUEUE` в `commit_owned`/`init_owned`; `switchboard_program` — `address = SB_PROGRAM_ID` |
| 187 | SW002 | critical | `staking/src/instructions/vouchers.rs:141` | `recent_slothashes` | FALSE | SlotHashes sysvar — адрес проверяется внутри CPI (chip_core `OpenVoucher`: `#[account(address = randomness::SLOT_HASHES_ID)]`) |
| 188 | SW024 | high | `staking/src/state.rs:166` | `self . total_weight` | FALSE | `state.rs:159` `if self.total_weight == 0 { return }` перед делением |
