[package]
name = "market"
version = "0.2.0"
description = "GUTTERCAPS marketplace: freeze-in-place listings, offers, fee split"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "market"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
# Cluster flags exist only so `anchor build -- --features devnet|localnet` builds the whole workspace;
# forwarded to chip_core for consistency (this program never reads randomness itself).
devnet = ["chip_core/devnet"]
localnet = ["chip_core/localnet"]
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build", "chip_core/idl-build"]

[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"] }
anchor-spl = "0.32.2"
# `>=0.11.1, <0.12`: 0.12 requires solana-program ^3, whose manifests the platform cargo (1.79) cannot
# read; the anchor feature exists since 0.11.1. scripts/ci-cargo-lock.sh pins the exact version.
mpl-core = { version = ">=0.11.1, <0.12", default-features = false, features = ["anchor"] }
mpl-bubblegum = "=2.1.1"
chip_core = { path = "../chip_core", features = ["cpi"] }
